local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_old_hoxton" and "Джеймс Хоксворт" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_spanish" and "Николос" or text_original(self, string_id, ...)	
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_jowi" and "Джонатан Баба Яга бугимен" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_female_1" and "Рошель" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_dragan" and "Мр.Зубовик" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_jacket" and "Ричард" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
	                return string_id == "menu_bonnie" and "Бонни МакГии" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_sokol" and "Сергей Козак" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_jimmy" and "Джеймс" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
                    return string_id == "menu_sydney" and "Келли Кинг" or text_original(self, string_id, ...)
end					

	

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	 return string_id == "menu_wild" and "Том Бишоп Сатана" or text_original(self, string_id, ...)               
end	


local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	  return string_id == "menu_terry" and "Тони Монтана Антонио" or text_original(self, string_id, ...)              
end	
	            
local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	 return string_id == "menu_max" and "Мачетозо Антонио Бенитес Родригес" or text_original(self, string_id, ...)               
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	 return string_id == "menu_myh" and "Август Линденхерст" or text_original(self, string_id, ...)               
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	   return string_id == "menu_ecp_male" and "Итан Клеин" or text_original(self, string_id, ...)             
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	  return string_id == "menu_ecp_female" and "Хила Клейн Хила Хакмон" or text_original(self, string_id, ...)              
end	
            
	
	 				