local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_old_hoxton" and "James Hoxworth" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_spanish" and "Nicolas" or text_original(self, string_id, ...)	
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_jowi" and "Jonathan" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_female_1" and "Rochelle" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_dragan" and "Zubovic" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	                return string_id == "menu_jacket" and "Richard" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
	                return string_id == "menu_bonnie" and "Bonnie McGee" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_sokol" and "Sergei Kozak" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...) 
                    return string_id == "menu_jimmy" and "James" or text_original(self, string_id, ...)
end

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
                    return string_id == "menu_sydney" and "Kelli King" or text_original(self, string_id, ...)
end					

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
                   return string_id == "menu_sydney" and "Kelli King" or text_original(self, string_id, ...) 
end	

local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	 return string_id == "menu_wild" and "Tom Bishop The Devil" or text_original(self, string_id, ...)               
end	


local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	  return string_id == "menu_terry" and "Tony Montana" or text_original(self, string_id, ...)              
end	
	            
local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	 return string_id == "menu_max" and "Machetazo Antonio Benítez Rodríguez" or text_original(self, string_id, ...)               
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	 return string_id == "menu_myh" and "August Lindenhurst" or text_original(self, string_id, ...)               
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	   return string_id == "menu_ecp_male" and "Ethan Klein" or text_original(self, string_id, ...)             
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	  return string_id == "menu_ecp_female" and "Hila Klein" or text_original(self, string_id, ...)              
end	
	 local text_original = LocalizationManager.text
    function LocalizationManager:text(string_id, ...)
	  return string_id == "menu_ecp_male" and "Ethan Klein" or text_original(self, string_id, ...)              
end	
	 				
